// En este archivo definirás tus rutas e importarás los componentes que vas a renderizar.
import { testingRouter } from './router.js';

document.addEventListener('DOMContentLoaded', () => {
  testingRouter();
});